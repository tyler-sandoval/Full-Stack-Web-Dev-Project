@model IEnumerable<FSDP.DATA.EF.CourseAssignment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AspNetUser.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cours.CourseName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUser.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cours.CourseName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CourseAssignments }) |
            @Html.ActionLink("Details", "Details", new { id=item.CourseAssignments }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CourseAssignments })
        </td>
    </tr>
}

</table>
