@model IEnumerable<FSDP.DATA.EF.LessonView>

@{
    ViewBag.Title = "Lessons Viewed";
}

<h2>@ViewBag.Title</h2>

@if (User.IsInRole("Admin"))
{
    <p class="btn btn-warning" id="prezStyle">
        @Html.ActionLink("Create New", "Create")
    </p>
    <hr />
}
<table class="table" id="DataTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateViewed)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lesson.LessonTitle)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UserID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DateViewed)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Lesson.LessonTitle)
                </td>
                <td>
                    <button class="btn btn-warning" id="prezStyle">
                        @Html.ActionLink("Details", "Details", new { id = item.LessonViewID })
                    </button>
                    @if (User.IsInRole("Admin"))
                    {
                        <button class="btn btn-warning" id="prezStyle">
                            @Html.ActionLink("Edit", "Edit", new { id = item.LessonViewID })
                        </button>
                        <button class="btn btn-warning" id="prezStyle">
                            @Html.ActionLink("Delete", "Delete", new { id = item.LessonViewID })
                        </button>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>

@section pageSpecificSlider{
    <div class="carousel-inner" role="listbox">
        <div class="item active">
            <img class="img-responsive" src="~/Content/img/1920x1080/fsdp.png" alt="Slider Image">
            <div class="container">
                <div class="carousel-centered">
                    <div class="margin-b-40">
                        <h1 class="carousel-title" style="text-shadow: 2px 2px 2px black, 0 0 25px purple, 0 0 10px indigo;">Lesson Views</h1>
                    </div>
                </div>
            </div>
        </div>
    </div>
}

@section scripts{
    <script>
        $(document).ready(function () {
            $('#DataTable').DataTable();
        });
    </script>
}